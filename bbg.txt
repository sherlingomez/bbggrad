<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Personalized Scroll with Hearts</title>

  <style>
    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      height: 100vh;
      overflow: hidden;
      display: flex;
      align-items: center;
      justify-content: center;
      font-family: "Times New Roman", serif;
      position: relative;
      background: #8b0000;
      background-image:
        radial-gradient(circle, white 1px, transparent 1px);
      background-size: 30px 30px; /* white hearts grid effect */
    }

    /* CLICK LABEL */
    #clickLabel {
      position: absolute;
      top: 50%;
      left: 50%;
      transform: translate(-50%, -50%);
      font-size: 2rem;
      color: #fff;
      cursor: pointer;
      text-shadow: 1px 1px 4px rgba(0,0,0,0.7);
      user-select: none;
      text-align: center;
      z-index: 2;
    }

    /* SCROLL */
    .scroll-container {
      overflow: hidden;
      width: 600px;
      height: 0;
      border-left: 8px solid #d4b56a;
      border-right: 8px solid #d4b56a;
      border-radius: 6px;
      background: #fffdf0;
      box-shadow: 0 8px 15px rgba(0,0,0,0.25);
      cursor: pointer;
      position: relative;
      z-index: 3;
      display: flex;
      align-items: flex-start;
      justify-content: flex-start; 
    }

    .scroll-text {
      padding: 2rem;
      text-align: left;
      font-size: 1.4rem; 
      color: #7a0000;
      line-height: 1.6;
      opacity: 0;
      white-space: pre-line;
      transition: opacity 0.5s ease;
      width: 100%;
    }

    /* UNROLL VERTICALLY */
    .scroll-container.reveal {
      animation: unroll 3s ease forwards;
    }

    @keyframes unroll {
      0%   { height: 0; }
      100% { height: 400px; } 
    }

    .scroll-container.reveal .scroll-text {
      opacity: 1;
      transition-delay: 3s;
    }

    /* HAND-DRAWN HEARTS */
    .doodle-heart {
      position: absolute;
      font-size: 1.4rem;
      color: #b30000;
      opacity: 0.85;
      transform: rotate(var(--rot));
      user-select: none;
    }

    .heart-1 { top: 5%; left: 5%; --rot: -10deg; }
    .heart-2 { bottom: 5%; left: 10%; --rot: 6deg; }
    .heart-3 { top: 5%; right: 5%; --rot: 12deg; }
    .heart-4 { bottom: 5%; right: 10%; --rot: -6deg; }

  </style>
</head>
<body>

  <div id="clickLabel">muthumani smileeeee</div>

  <div class="scroll-container" id="scroll">
    <div class="scroll-text" id="scrollText">
Sreyas bbg,
So proud of you on completing your paditham, maybe all your dreams come true. I hope you are happy, may god bless you. Congratulations on graduating, ente shuttumanimwahahahaha xxxx Treat pinne mathi
With love,  
Sherliii <3
    </div>
  </div>

  <script>
    const scroll = document.getElementById("scroll");
    const scrollText = document.getElementById("scrollText");
    const label = document.getElementById("clickLabel");

    label.addEventListener("click", () => {
      label.style.display = "none"; 
      scroll.classList.add("reveal");

      setTimeout(() => addHearts(), 3000); 
    });

    scroll.addEventListener("click", () => {
      if (!scroll.classList.contains("reveal")) {
        label.click(); 
      }
    });

    function addHearts() {
      const hearts = [
        { cls: "doodle-heart heart-1", text: "♡" },
        { cls: "doodle-heart heart-2", text: "♥" },
        { cls: "doodle-heart heart-3", text: "♡" },
        { cls: "doodle-heart heart-4", text: "♥" }
      ];

      hearts.forEach(h => {
        const el = document.createElement("div");
        el.className = h.cls;
        el.textContent = h.text;
        scroll.appendChild(el);
      });
    }
  </script>

</body>
</html>
